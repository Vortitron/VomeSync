# One leftover working copy from the split to Vortitron/VomeSync-server.
# The cutover is done -- every live and dev container now runs from
# /var/www/VomeSync-server/docker -- so the rest of the copies have been
# deleted.  This one stays because it is not purely a copy: it holds a
# webserver/.env the server repo has no equivalent of, and 155 files under
# webserver/media where the live vomesync_webserver_media volume has two.
# Work out whether that media needs migrating before deleting this.
/webserver/
node_modules
venv
.redisdata/
custom_components/vomesync/__pycache__/
hacs-addon/tests/__pycache__/
vome/custom_components/vomesync/__pycache__/
__pycache__/
.pytest_cache
.coverage
coverage.xml
.agents/skills/*
skills-lock.json
*/.env
*/*/.env
backups/*
